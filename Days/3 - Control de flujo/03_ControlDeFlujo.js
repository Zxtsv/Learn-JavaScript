// ! Estructura IF

let condicion = true;

if (condicion) {
    console.log("Entre al if");
}
else{
    console.log("No entre al if");
}

// ! Estructura switch
let condicion = 1;
switch (condicion) {
    case 1:
        // Codigo
        break; // Identifica el termino de un caso

    default:
        // Código en caso de que no se cumpla ningún caso
        break;
}

